# 📱 EAS Build Guide for You Can FI

## ✅ Project Setup Complete

- **EAS Project**: `@logi-dev/you-can-fi`
- **Project ID**: `9ba18edf-bf40-443a-9670-fe4afc89c02b`
- **Project URL**: https://expo.dev/accounts/logi-dev/projects/you-can-fi
- **Status**: ✅ Linked and ready for builds

## 🚀 Quick Start: Build Your App

### Option 1: Interactive Build (Recommended)

Run the build command interactively - it will guide you through credential setup:

```bash
# For Android APK (Preview)
eas build --platform android --profile preview

# For Android App Bundle (Production)
eas build --platform android --profile production

# For iOS (Preview)
eas build --platform ios --profile preview

# For iOS (Production - requires Apple Developer account)
eas build --platform ios --profile production

# For both platforms
eas build --platform all --profile preview
```

### Option 2: Configure Credentials First

If you want to set up credentials before building:

```bash
# Configure Android credentials
eas credentials --platform android

# Configure iOS credentials (requires Apple Developer account)
eas credentials --platform ios
```

## 📋 Build Profiles

### Preview Profile
- **Android**: APK file (easy to install and test)
- **iOS**: Simulator or device build
- **Distribution**: Internal (for testing)
- **Use Case**: Testing with team members, QA

### Production Profile
- **Android**: App Bundle (AAB) for Google Play Store
- **iOS**: App Store build
- **Distribution**: Store
- **Use Case**: App Store/Play Store submission

## 🔐 Credentials Setup

### Android Credentials
- **Keystore**: Auto-generated by Expo (recommended)
- **Alternative**: Provide your own keystore
- **Location**: Managed by Expo servers (secure)

### iOS Credentials
- **Apple Developer Account**: Required for production builds
- **Certificates**: Auto-managed by Expo
- **Provisioning Profiles**: Auto-generated

## 📦 Build Outputs

After a successful build, you'll get:

1. **Download Link**: Direct download URL for the build
2. **QR Code**: Scan to install on device (Android)
3. **Build Logs**: Full build output for debugging

## 🎯 Recommended Build Flow

### Step 1: First Build (Preview)
```bash
eas build --platform android --profile preview
```
This will:
- Generate Android keystore automatically
- Create an APK you can install directly
- Take ~10-15 minutes

### Step 2: Test the Build
- Download the APK from the build page
- Install on Android device
- Test all features with production API

### Step 3: Production Build (When Ready)
```bash
eas build --platform android --profile production
```
This creates an App Bundle (AAB) for Google Play Store submission.

## 🌐 Environment Variables

If you need to set environment variables for builds:

```bash
# Set environment variables via EAS
eas secret:create --scope project --name API_BASE_URL --value "https://you-can-fi-production.up.railway.app/api/v1" --type string

# Or use .env files (for local builds)
```

## 📊 Build Status

Monitor your builds at:
- **Dashboard**: https://expo.dev/accounts/logi-dev/projects/you-can-fi/builds
- **CLI**: `eas build:list`

## 🔧 Troubleshooting

### Build Fails
1. Check build logs: `eas build:view [BUILD_ID]`
2. Verify `app.json` configuration
3. Check for missing dependencies

### Credential Issues
1. Run `eas credentials --platform android` to check status
2. Use Expo managed credentials (recommended)
3. Contact support if issues persist

### Local Builds
For faster iteration, use local builds:
```bash
eas build --platform android --profile preview --local
```
Requires Android SDK and build tools installed.

## 📱 Next Steps After Build

1. **Download the APK/AAB**
2. **Install on device** (Android) or TestFlight (iOS)
3. **Test all features**:
   - Onboarding flow
   - API connectivity
   - Asset/Liability management
   - Net Worth calculations
4. **Submit to stores** (when ready):
   - Google Play Console (Android)
   - App Store Connect (iOS)

## 🔗 Useful Commands

```bash
# View all builds
eas build:list

# View specific build
eas build:view [BUILD_ID]

# Cancel a build
eas build:cancel [BUILD_ID]

# Download build
eas build:download [BUILD_ID]

# Check build status
eas build:status
```

## 📚 Resources

- **EAS Build Docs**: https://docs.expo.dev/build/introduction/
- **Build Configuration**: https://docs.expo.dev/build/eas-json/
- **Credentials Management**: https://docs.expo.dev/app-signing/managed-credentials/
- **Project Dashboard**: https://expo.dev/accounts/logi-dev/projects/you-can-fi

---

## 🎉 Ready to Build!

Your project is fully configured. Run:
```bash
eas build --platform android --profile preview
```

And follow the interactive prompts to generate credentials and start your first build!

