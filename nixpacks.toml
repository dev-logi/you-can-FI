[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = [
  "npm install --legacy-peer-deps"
]

[phases.build]
cmds = [
  "npm run build:web"
]

[start]
cmd = "npx serve dist -l tcp://0.0.0.0:$PORT"

